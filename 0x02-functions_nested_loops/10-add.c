#include "main.h"

/**
 * add - This function adds two integers and returns the result
 * @a: First operand
 * @b: Second opernad
 *
 * Return: The sum of the two operads
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
